version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No pre-build steps required for static HTML"
    build:
      commands:
        - echo "No build process required for static HTML"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []